PORT=8787
# --- Proxy will use these keys (never expose to frontend) ---
# --- sh_ai modes (SECRET — never expose to frontend, server-side only) ---
# Flash (Groq API)
# GROQ_API_KEY=
# GROQ_MODEL=openai/gpt-oss-20b
# Deep (OpenRouter API, модель підтягується з апі автоматично)
# OPENROUTER_API_KEY=
# OPENROUTER_MODEL=openrouter/free
# Uncensored (твоя Ollama на приватному сервері — заповниш пізніше)
# OLLAMA_URL=https://твій-приватний-сервер:11434
# OLLAMA_MODEL=llama3.1:8b
# --- CORS: comma-separated origins, * for dev ---
# ALLOWED_ORIGINS=*
